{
  "name": "Demo16",
  "description": "A demo showing deleting ALL workflow instances",
  "tasks":{
    "DELETE ALL Workflows": {
      "description": "Delete all workflows, except this one of course :-)",
      "blocking": true,
      "handler" : "../taskhandlers/execHandler",
      "data": {
        "cmd": "./bin/processus-cli --deleteALL -l info"
      }
    }
  }
}
